
'use strict';

describe('myApp.Leaderboard module', function() {

  beforeEach(module('myApp.Leaderboard'));

  describe('Leaderboard controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var LeaderboardCtrl = $controller('LeaderboardCtrl');
      expect(LeaderboardCtrl).toBeDefined();
    }));

  });
});
                        
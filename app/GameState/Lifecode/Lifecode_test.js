
'use strict';

describe('myApp.Lifecode module', function() {

  beforeEach(module('myApp.Lifecode'));

  describe('Lifecode controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var LifecodeCtrl = $controller('LifecodeCtrl');
      expect(LifecodeCtrl).toBeDefined();
    }));

  });
});
                        
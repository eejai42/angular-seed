
'use strict';

describe('myApp.Redeem module', function() {

  beforeEach(module('myApp.Redeem'));

  describe('Redeem controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var RedeemCtrl = $controller('RedeemCtrl');
      expect(RedeemCtrl).toBeDefined();
    }));

  });
});
                        

'use strict';

describe('myApp.Story module', function() {

  beforeEach(module('myApp.Story'));

  describe('Story controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var StoryCtrl = $controller('StoryCtrl');
      expect(StoryCtrl).toBeDefined();
    }));

  });
});
                        
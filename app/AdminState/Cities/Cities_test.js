
'use strict';

describe('myApp.Cities module', function() {

  beforeEach(module('myApp.Cities'));

  describe('Cities controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var CitiesCtrl = $controller('CitiesCtrl');
      expect(CitiesCtrl).toBeDefined();
    }));

  });
});
                        
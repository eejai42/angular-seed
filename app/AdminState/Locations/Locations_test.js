
'use strict';

describe('myApp.Locations module', function() {

  beforeEach(module('myApp.Locations'));

  describe('Locations controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var LocationsCtrl = $controller('LocationsCtrl');
      expect(LocationsCtrl).toBeDefined();
    }));

  });
});
                        